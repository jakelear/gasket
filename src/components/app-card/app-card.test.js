import React from 'react';
import { shallow } from 'enzyme';
import AppCard from './app-card.component';

describe('AppCard', () => {
  it('renders', () => {
    const component = shallow(<AppCard />);
    expect(false).toEqual(true);
  });
});

import React from 'react';
import { shallow } from 'enzyme';
import AppBackground from './app-background.component';

describe('AppBackground', () => {
  it('renders', () => {
    const component = shallow(<AppBackground />);
    expect(false).toEqual(true);
  });
});

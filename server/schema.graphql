type Query {
  applist(filter: String!): AppList,
  appNews(appid: ID!): AppNews,
  appDetails: App
}

type AppList {
  apps: [App]
}

type App {
  appid: ID
  name: String
}

type AppNews {
  newsitems: [NewsItem]
}

type NewsItem {
  title: String
  url: String
}